name: ci
on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 9.12.0 }
      - uses: actions/setup-node@v4
        with: { node-version: 22, cache: pnpm }
      - run: pnpm install --frozen-lockfile
      - run: pnpm lint
      # Build BEFORE typecheck so workspace packages' .d.ts files exist —
      # core's typecheck imports types from @specgen/parser-frontend-capture/dist
      # via `typeof import(...)`, and tsc can't resolve it without the dist.
      - run: pnpm -r --filter './packages/*' build
      - run: pnpm typecheck
      - run: pnpm test
